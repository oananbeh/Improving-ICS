# CamouflageNet — Python dependencies
# Install with:  pip install -r requirements.txt

# Core scientific stack
numpy>=1.24.0
scipy>=1.10.0

# Machine Learning (required)
scikit-learn>=1.3.0          # k-Means, Random Forest, Silhouette score
                              # Paper: "ML Engine: Scikit-learn 1.3 (k-center implementation)"

# Visualisation
matplotlib>=3.7.0

# Deep Learning (optional — for LSTM and Autoencoder with PyTorch backend)
# If not installed, the system falls back to MLP / PCA implementations
# torch>=2.0.0

# Data handling
pandas>=2.0.0                # optional, used for result export only

# Progress bars (optional)
tqdm>=4.65.0
